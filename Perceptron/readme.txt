Author:ZHOU
===========

IDE:pycharm
-----------

#####description
input:[[(x1,x2),y],......] //x1 and x2 are training data(2 dims), y is label 
output:w[],b,animation //w[] is the vector of weights(2 dims),b is bias,animation is the visulization of training process
